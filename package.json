{
  "name": "niyati-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Niyati OS - A pixel-art fashion portfolio",
  "scripts": {
    "prestart": "cd frontend && npm install --legacy-peer-deps",
    "start": "cd frontend && npm start",
    "prebuild": "cd frontend && npm install --legacy-peer-deps",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install --legacy-peer-deps"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
